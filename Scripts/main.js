nova.commands.register('SortLines.sortLinesAsc', (workspace) => {});

nova.commands.register('SortLines.sortLinesDesc', (workspace) => {});

nova.commands.register('SortLines.reverseLines', (workspace) => {});
